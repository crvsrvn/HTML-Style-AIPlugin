---
name: html-style
description: 创建、修改、重排或发布任何 HTML 成品时必须使用——包括 HTML 文档、报告、方案、说明页、仪表盘、Claude Artifacts、把 Markdown 转成 HTML、更新已有 Artifact 链接。统一样式：Comic Sans MS 16px、右侧大纲、底色 #FCFCFB / #151515、988px 居中版心；用图标、语义提示块、状态标签、图例记号、ID 着色辅助阅读；SVG 等图表可左右键拖动、滚轮定点缩放、双击全屏，并尽量用原生 HTML/CSS 提升可读性；成品托管到 AI 云端（如 Claude Artifacts），不交付本地 .html 文件。
---

# HTML 样式规范

本规范是用户对 HTML 样式的明确约定：

- 用户在当次请求里给出的不同要求，优先于本规范。
- 本规范优先于通用设计建议，例如 artifact-design 等技能里关于字体、配色、版式的默认建议，也优先于更早的零散约定（如"左侧放大纲"）。
- 本规范不覆盖宿主平台的硬性约束，例如 Artifact 的 CSP 白名单、主题三态、安全区。

## 适用范围

- 新建或修改任何 HTML 成品。
- 修改已有页面时，把整页迁移到本规范，不只改动到的那一段。
- 不适用：用户项目里的网页源码（产品前端、组件、模板引擎文件）。它们遵循项目自身的设计系统。

## 硬性规范

| 项 | 要求 |
|----|------|
| 字体 | 正文 `Comic Sans MS`，16px；回退链见模板 `--hs-font`（Comic Neue → Chalkboard SE → 系统中文字体） |
| 背景 | 页面底色浅色 `#FCFCFB`，深色 `#151515`，跟随系统 / 宿主主题 |
| 大纲 | 放在右侧：固定的整高侧栏，不加底色；由模板脚本从标题生成，可筛选，随滚动用强调色文字和左侧细条标出当前位置；视口窄于 900px 时收进右上角「大纲」按钮，从右侧滑出。<br>折叠 / 展开：<br>• 默认折叠：显示第一层标题，以及当前阅读位置所在的路径<br>• 当前位置来自点击大纲或正常滚动；它和它的各级上级标题都展开下一层子标题，其余分支收起，逐级递归（例：到 2.2 时显示 2.1、2.2、2.3 与 2.2.1、2.2.2、2.2.3，2.1 的子标题收起）<br>• 还没读到任何标题时只显示第一层<br>• 标题栏右侧的纯图标按钮切换「展开全部 / 折叠」，展开时显示全部标题<br>• 筛选时显示全部匹配项；当前位置所属的第一层标题始终带竖线 |
| 版心 | 固定宽度 988px，在大纲左侧的区域内水平居中；空间不足时收窄，两侧至少留 16px |
| 标题 | 章节标题带序号：`h2` 写 `1.`，`h3` 写 `1.1`，`h4` 写 `1.1.1`，统一用 `<span class="hs-num">1.</span>` 包裹；序号与标题文字同色；标题不加图标 |
| 页内跳转 | 大纲条目、正文与表格里的页内锚点链接（`href="#…"`）点击后直接跳转，不做平滑滚动；只有「回到顶部」按钮保留平滑滚动。由模板脚本统一处理，不要另写滚动逻辑 |
| 强调 | 加粗与高亮二选一，按需选择，同一处不能既加粗又高亮：术语、关键词用 `<strong>`；需要一眼扫到的结论句用 `<mark>`，背景覆盖整段文字。加粗不带背景色 |
| 图表 | SVG、图片化图表、canvas、Mermaid 一律放进 `.hs-zoom` 视窗：<br>• 左键、右键都能拖动平移<br>• 滚轮在任意位置以指针为中心缩放<br>• 双击进入全屏，全屏里同样可拖动、缩放<br>• 再次双击或按 Esc 退出全屏<br>• 「复位」按钮回到适应尺寸 |
| 色彩与标记 | 必须用多种颜色、图标、标记辅助阅读（见[色彩、图标与标记](#色彩图标与标记)）：提示性段落用语义提示块；状态词用状态标签；图例符号用记号。颜色必须编码含义，不做纯装饰 |
| 可读性 | 尽量使用原生 HTML / CSS 能力让文档易读（见[原生可读性增强](#原生可读性增强)）；能用原生能力实现的交互，不写脚本 |
| 交付 | 托管到 AI 云端并给出链接，不把本地 `.html` 当成品（见[托管](#托管)） |

其余视觉参数（配色、字号层级、表格与代码块样式）以模板为准，不要另起一套。

## 色彩、图标与标记

颜色分两类，每类只表达一种含义；其余元素（标题色条、表头、列表符号、大纲当前位置）统一用强调色 `--hs-accent`：

| 类别 | 取值 | 含义 |
|------|------|------|
| 语义色 | `data-kind`：`info` 说明（蓝）、`tip` 建议（绿）、`ok` 通过（绿）、`warn` 注意（琥珀）、`danger` 风险（红）、`key` 要点（紫）、`muted` 备注（灰） | 内容的性质或状态 |
| ID 色 | 脚本从 `--hs-c1` … `--hs-c7` 按前缀出现顺序分配 | 同一前缀的编号（`RT-01`、`RT-02`）同色，不同前缀异色 |

标记写法：

| 标记 | 写法 | 用在哪里 |
|------|------|----------|
| 提示块 | `<blockquote class="hs-callout" data-kind="warn"><p class="hs-callout-title">注意</p>…</blockquote>` | 说明、建议、注意事项、风险、结论要点。标题用 2–4 个字概括 |
| 键值说明 | 提示块内 `<dl class="hs-kv"><dt>来源</dt><dd>…</dd></dl>` | 文档元信息、配置说明 |
| 状态标签 | `<span class="hs-tag" data-kind="ok">已修</span>` | 表格状态列、优先级、段落里的状态词 |
| 图例记号 | `<span class="hs-mark" data-kind="warn" title="换表达">≈</span>` | 文档自定义的分类符号；`title` 写符号含义，并在首次出现处给图例 |
| ID 标记 | 行内 `<code>RT-01</code>` | 需求、缺口、决策编号；脚本自动着色，不用手写类名 |
| 行内图标 | `<i class="hs-ico" data-icon="warn"></i>` | 需要在正文里点缀图标时 |

可用图标：`info` `tip` `ok` `warn` `danger` `flag` `note` `question` `book` `target` `compare` `database` `flow` `code` `image` `list` `calendar` `flask` `layers` `branch` `clock` `link` `zap` `box` `shield` `migrate` `search` `arrow-up` `expand` `shrink` `reset` `unfold` `fold`。

原则：

- 状态词的语义固定：完成 / 已修 / 已有 → `ok`；新增 / 保留 → `info`；未做 / 未实现 / 中优先级 → `warn`；删除 / 缺口 / 高优先级 → `danger`；不变 / 同上 / 无 → `muted`。
- 提示块只用于需要读者停下来注意的段落，一屏内不超过两个。
- 不用 emoji 充当图标。

## 原生可读性增强

以下能力都不依赖脚本，按内容需要自由组合。

| 能力 | 写法 | 用在哪里 |
|------|------|----------|
| 折叠代码 | `<details class="hs-code" [open]><summary><span class="hs-code-lang">C#</span><span class="hs-code-meta">N 行</span></summary><pre><code>…</code></pre></details>` | 带语言标注的代码块；超过 25 行默认折叠，其余默认展开 |
| 折叠段落 | `<details><summary>标题</summary>…</details>` | 附录、推导过程、次要细节、长清单 |
| 术语表 | `<dl><dt>术语</dt><dd>解释</dd></dl>` | 术语、字段、参数说明 |
| 脚注弹层 | `<button type="button" class="hs-ref" popovertarget="fn-1">1</button>` + `<div popover id="fn-1" class="hs-pop">…</div>` | 脚注、补充解释，不打断正文 |
| 行内语义 | `<kbd>`、`<mark>`、`<abbr title="…">` | 按键、重点句（与加粗二选一）、缩写 |
| 表格 | `<div class="hs-table"><table><caption>…</caption>…</table></div>` | 模板自带表头底色、斑马纹、悬停高亮、窄屏横向滚动 |
| 标题锚点 | `<a class="hs-anchor" href="#id">#</a>` | 长文档里可分享的章节链接 |
| 回到顶部 | 模板里的 `.hs-top` 标记 | 所有长文档，纯 CSS 滚动后淡入 |
| 跳转高亮 | 模板自带 `:target` 样式 | 从大纲或链接跳转后短暂高亮目标 |

原则：

- 折叠只用于次要内容；结论、关键数据不折叠。
- 不要把正文拆成只能靠脚本显示的片段。`<details>` 的内容在浏览器页内搜索中仍可命中。

## 工作流程

### 新建页面

1. 读取本技能目录下的 `assets/template.html`。
2. 以下几块原样复制，不要改写、删减或"重新实现"：
   - 字体 `<link>`
   - `<style id="hs-style">`
   - `<a class="hs-top">` 回到顶部标记
   - `<script id="hs-script">`
3. 正文全部写在 `<main class="hs-doc">` 里：
   - 一个 `h1` 作标题，`id="top"`，其后可跟 `<p class="hs-meta">` 写日期、来源。
   - 章节用 `h2` / `h3`，默认这两级进大纲；要收录 `h4` 时，给 `main` 加 `data-toc-levels="2,3,4"`。
   - 不要手写大纲，脚本会生成。
4. 图表写成 `<figure><div class="hs-zoom">img / svg / canvas</div><figcaption>…</figcaption></figure>`：
   - `figure` 的直接子级 img / svg / canvas，以及 Mermaid 渲染出的 SVG，会被脚本自动包进 `.hs-zoom`。
   - 底板：图片默认白色底板，适合 PlantUML、Mermaid 导出图、截图这类自带白底和深色线条的图。
   - 内联 SVG 和 canvas 默认用面板色底板，要求用 `var(--hs-*)` 跟随主题配色。
   - 需要反过来时，在 `.hs-zoom` 上设 `data-plate="light"` 或 `data-plate="none"`。
5. 按[色彩、图标与标记](#色彩图标与标记)和[原生可读性增强](#原生可读性增强)组织内容：标题带序号、不加图标，把提示性段落写成提示块，把状态词写成状态标签，强调时在加粗与高亮之间二选一。
6. 页面特有样式另起一个 `<style>`：只用 `--hs-*` 令牌取色，不覆盖硬性项。
7. 宿主要求完整文档时（非 Artifact），外面包一层 `<!doctype html><html lang="zh-CN"><head>…</head><body>…</body></html>`：
   - `head` 里放 `<meta charset="utf-8">`、`<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">`，以及 `title`、字体 `link`、`style`。
   - 其余标记和 `script` 放在 `body` 里，`script` 放在末尾。

### 修改已有页面

1. 先取回线上最新版本，基于它修改。Claude Artifact 用 Artifact 工具 `action: "read"` 读取。
2. 保留全部内容：文字、标题 `id`、页内锚点、表格、代码高亮、图片数据、附带文件（如 PlantUML 源文件）。
3. 删除以下旧结构：
   - 旧字体链接和全局样式。
   - 旧大纲，包括目录、筛选框和目录脚本。
   - 旧的图片缩放、切换按钮及其脚本。
4. 按"新建页面"第 2–6 步套上模板：
   - 标题序号统一写成 `hs-num`，缺序号的补上；去掉标题图标。
   - 旧类名换成模板类名，例如锚点 → `hs-anchor`，表格容器 → `hs-table`，带语言标注的代码块 → `details.hs-code`。
   - 模板没有对应物的旧类名，在页面特有样式里改用 `--hs-*` 令牌。
   - 补齐标记：逐个判断旧引用块的性质，改成对应 `data-kind` 的提示块并加标题；表格里整格是状态词的改成状态标签；文档自定义的图例符号改成图例记号。
5. 页面较大（例如含大量 base64 图）时，用脚本做结构替换，不要手工逐段改。替换前后核对标题、表格、代码块、图片的数量一致。

## 托管

- 中间文件只放在会话临时目录（Claude Code 的 scratchpad 或系统临时目录），不放进用户项目。
- **Claude Code / Claude 桌面版**：
  - 用 Artifact 工具发布，回复里给链接。
  - 更新已有 Artifact 时传同一个 `url`，或在同一会话里用同一个文件路径，保持链接不变。
  - 遵守该工具的页面契约：不写 doctype / html / head / body；外部样式只从 Google Fonts 加载。
- **claude.ai 网页**：直接产出 HTML Artifact。
- **Codex**：有 `sites-hosting`（配合 `sites-building`）等托管技能时，用它发布并给出链接。没有可用的托管能力时，明确告诉用户"当前环境无法托管"，给出临时目录里的文件路径，不要声称已发布。
- 用户明确要求本地文件时，按用户要求交付。

## 发布前自检

- [ ] 正文字体为 Comic Sans MS（或其回退），16px。
- [ ] 页面底色为 `#FCFCFB` / `#151515`，浅色和深色主题下文字都清晰。
- [ ] 视口 ≥900px 时大纲是右侧整高侧栏，没有底色，默认折叠并随阅读位置逐级展开，折叠 / 展开按钮只有图标；更窄时有「大纲」按钮和右侧抽屉。
- [ ] 版心最宽 988px 并居中，页面没有横向滚动；只有表格、代码块、图表在自己的容器内滚动或缩放。
- [ ] 每个图表都能左键、右键拖动，滚轮以指针为中心缩放，双击全屏，全屏内同样可拖动缩放，双击或 Esc 退出。
- [ ] 标题都带序号且序号不着色，没有图标；加粗与高亮没有叠用。
- [ ] 提示块、状态标签、图例记号、ID 标记都已用上，且每种颜色都有明确含义。
- [ ] 长代码、附录等次要内容已用原生折叠；没有为原生能力可以实现的交互写脚本。
- [ ] 交付的是云端链接，或已如实说明无法托管。
